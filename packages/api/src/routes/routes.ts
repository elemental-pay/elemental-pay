// import userController from '../controllers/user.controller';

const homeTimeline = { get: () => {} };

const routes = {
  // '/board/:id': {
  //   GET: {
  //     handler: userController.get,
  //     validation: userController.validation,
  //   },
  // },
};

export default routes;
